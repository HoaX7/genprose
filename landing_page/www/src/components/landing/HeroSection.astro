---
import Stepper from "./Stepper.astro";
---

<div class="container p-4 mx-auto" id="hero">
    <div class="mt-5 md:mt-24 relative">
        <div class="grid grid-cols-5">
            <div class="col-span-5 lg:col-span-3 flex justify-between flex-col">
                <div class="md:text-6xl text-4xl font-bold mt-10">
                    <img alt="genprose" src="/assets/images/logo-white-img.png" width="42" class="inline" />
                    Gen Prose
                </div>
                <h1 class="text-2xl font-semibold mt-3 flex flex-col">
                    <span class="">Instantly Transmute <sup class="text-sm">(Transform)</sup></span>
                    <span class="underline">Audio, Video and Webinars</span>
                    <span class="">into SEO optimized content.</span>
                </h1>
                <p class="mt-5 text-2xl">
                    As simple as
                <div class="lg:gap-8 stepper-bg justify-center mt-3 p-5 py-8 rounded-lg gap-4 flex flex-col md:flex-row"
                    id="stepper">
                    <Stepper text="Paste your link" />
                    <Stepper text="Get your content" />
                    <Stepper text="Publish your content" />
                </div>
                </p>
            </div>
            <div class="hidden lg:block col-span-2">
                <img src="/assets/images/writer-1.svg" alt="transcribe audio" />
            </div>
        </div>
        <div class="mt-5 font-semibold text-center">
            Create limitless SEO optimized content at the blink of an eye with different personas.
        </div>
        <div class="py-8 flex items-center justify-center">
            <a class="rounded-full py-5 cta-btn px-10 cursor-pointer font-semibold" href="#joinwaitlist">
                Join the waitlist
            </a>
        </div>
    </div>
</div>